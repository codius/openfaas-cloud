apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: edge-router
  namespace: openfaas
spec:
  policyTypes:
    - Ingress
  podSelector:
    matchLabels:
      app: edge-router
  ingress:
  - from:
    - namespaceSelector: {}
      podSelector:
        matchLabels:
          app.kubernetes.io/name: ingress-nginx
